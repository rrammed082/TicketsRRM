<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TicketsRRM"
        tools:targetApi="31">
        <activity
            android:name=".activities.UpdateDetailTicketActivity"
            android:exported="false" />
        <activity
            android:name=".activities.UpdateTicketActivity"
            android:exported="false" />
        <activity
            android:name=".activities.CreateDetailTicketActivity"
            android:exported="false" />
        <activity
            android:name=".activities.DetailsTicketsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.CreateTicketActivity"
            android:exported="false" />
        <activity
            android:name=".activities.DetailTicketActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>